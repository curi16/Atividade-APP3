import React from "react";
import { View } from "react-native";
import styled, { ThemeProvider } from "styled-components/native";

const theme = {
  colors: {
    primary: "#6200ee",
    text: "#333",
    white: "#fff",
  },
};

const Card = styled.View`
  background-color: ${(props) => props.theme.colors.white};
  padding: ${(props) => props.$spacing || "16px"};
  border-radius: 12px;
  elevation: 4;
`;

const Typography = styled.Text`
  font-size: ${(props) => (props.h1 ? "24px" : "16px")};
  font-weight: ${(props) => (props.h1 ? "bold" : "normal")};
  color: ${(props) => props.theme.colors.text};
`;

const StyledButton = styled.Pressable`
  background-color: ${(props) => props.theme.colors.primary};
  padding: 12px;
  border-radius: 8px;
  margin-top: 10px;
`;

export default function App() {
  return (
    <ThemeProvider theme={theme}>
      <View
        style={{
          flex: 1,
          justifyContent: "center",
          padding: 20,
          backgroundColor: "#f0f0f0",
        }}
      >
        <Card $spacing="30px">
          <Typography h1>H1 Título</Typography>
          <Typography>parágrafo</Typography>
          <StyledButton>
            <Typography style={{ color: "#fff", textAlign: "center" }}>
              Botão
            </Typography>
          </StyledButton>
        </Card>
      </View>
    </ThemeProvider>
  );
}
